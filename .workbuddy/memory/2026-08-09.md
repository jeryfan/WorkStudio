# 2026-08-09

- 排查侧边栏鼠标悬浮误显示小手（cursor: pointer）问题：根源是 `src/renderer/src/assets/main.css` 里全局 `button { cursor: pointer }` 规则，导致无动作的占位按钮也显示小手。
- 按用户要求删除了该全局规则，暂不补充任何小手样式。显式写了 `cursor-pointer` 的位置（SectionHeader 折叠按钮、ProjectRow 整行）保留未动，如后续要去掉可再处理。
